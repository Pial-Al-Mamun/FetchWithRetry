{
  "name": "@pial/fetch-retry",
  "version": "1.0.0",
  "description": "Minimal fetch wrapper with retry, backoff, and jitter",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --minify && tsc --emitDeclarationOnly --declaration",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "prepublishOnly": "bun test && bun run build"
  },
  "keywords": [
    "fetch",
    "retry",
    "jitter",
    "backoff",
    "typescript"
  ],
  "author": "Pial Al Mamun",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pial/fetch-retry"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
